#include  <stdio.h>
int main() {
    int age;
    float per;
    printf ("enter your age ");
    scanf ("%d"   ,& age );
    printf("enter your per ");
    scanf ("%f ",&per);
    (age>=18 || per>=40)? printf ("welcome"): printf ("sorry");
    return 0;
}
